<%= form_for(@similar_image) do |f| %>
  <% if @similar_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@similar_image.errors.count, "error") %> prohibited this similar_image from being saved:</h2>

      <ul>
      <% @similar_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_msts_id %><br>
    <%= f.number_field :image_msts_id %>
  </div>
  <div class="field">
    <%= f.label :keyword_msts_id %><br>
    <%= f.number_field :keyword_msts_id %>
  </div>
  <div class="field">
    <%= f.label :inner_product %><br>
    <%= f.text_field :inner_product %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
