<%= form_for(@keyword_ignore) do |f| %>
  <% if @keyword_ignore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyword_ignore.errors.count, "error") %> prohibited this keyword_ignore from being saved:</h2>

      <ul>
      <% @keyword_ignore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_mst_id %><br>
    <%= f.number_field :image_mst_id %>
  </div>
  <div class="field">
    <%= f.label :keyword_mst_id %><br>
    <%= f.number_field :keyword_mst_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
